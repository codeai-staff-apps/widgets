/**
 * The widget's starting message and keyword, kept separate from `copy.ts`
 * because they are sample *data* to cipher, not interface text — but like
 * every other user-facing string, they're centralized here for a future
 * translation pass rather than sprinkled through JSX.
 */
export const sampleTexts = {
  message: 'If only this message could be a secret',
  keyword: 'my secret key',
};
